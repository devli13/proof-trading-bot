# dependencies
node_modules/

# build output
dist/
.vercel/

# secrets & wallet keys — NEVER commit
.env
.env.local
.env.*.local
.keys/
*.key

# logs
*.log

# os
.DS_Store
.vercel

# local bot registry seed (keys — never commit)
bots.json

# supabase CLI local files (config/auth/temp) — never commit
supabase/
.branches/
.temp/
